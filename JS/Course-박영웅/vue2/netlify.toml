[build]
    command = "npm run build"
    functions = "heropy"
    publish = "dist"

[dev]
    framework = "#custom"
    command= "npm run dev"
    targetPort = 8070
    port = 8080
    publish = "dist"
    autoLaunch = false